schema {
  query: Query
}

type AttributeType {
  name: String!
  value: String!
}

type ProductType {
  name: String!
  description: String!
  type: String!
  attributes: [AttributeType]
  id: String
}

type Query {
  product(id: String): ProductType
  products: [ProductType]
}
